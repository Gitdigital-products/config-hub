Rule: secrets ≠ configs.

v1 approach

encrypted = true → value encrypted at rest

Use aes-gcm or ring


aes-gcm = "0.10"

Encryption key from env:

CONFIG_HUB_MASTER_KEY=base64key

Long‑term:

HashiCorp Vault

AWS Secrets Manager

SOPS