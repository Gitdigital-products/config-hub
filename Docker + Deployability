Dockerfile

FROM rust:1.76 as builder
WORKDIR /app
COPY . .
RUN cargo build --release

FROM debian:bookworm-slim
COPY --from=builder /app/target/release/config-hub /usr/local/bin/config-hub
CMD ["config-hub"]

Why this matters

Works locally

Works in CI

Works in Kubernetes
